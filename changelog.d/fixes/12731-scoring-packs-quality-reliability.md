- **fix(auto-combo):** every mode pack now carries `quality` and `reliability` weights (`quality-first 0.03` parité DEFAULT, others `0.02`; `reliability-first 0.04` vitrine, others `0.03`). Pack sums stay `0.9999`; `health`/`stability` untouched; `DEFAULT reliability 0` unchanged. Docs `AUTO-COMBO.md` and diagram `f16` aligned. ([#12731](https://github.com/diegosouzapw/OmniRoute/pull/12731)) — thanks @maxmad64bis
