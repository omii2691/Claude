- **fix(sse):** strip the internal context-relay / universal-handoff flags (`_omnirouteSkipContextRelay`, `_omnirouteInternalRequest`, `_omnirouteSkipUniversalHandoff`) from the request body before it is dispatched upstream — handoff summary calls forwarded them to strict OpenAI-compatible providers (NVIDIA NIM, Groq), which reject the payload with HTTP 400 "Unsupported parameter(s)" and cost a wasted upstream round-trip per combo step ([#12729](https://github.com/diegosouzapw/OmniRoute/issues/12729))
